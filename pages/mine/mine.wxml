<import src="../template/simple-row.wxml" />

<view class="container">
  <view
    wx:for="{{pageData}}"
    wx:key="{{item.id}}"
    class="sp-border show-block"
  >
    <template is="simple-row" data="{{rowData: item}}" />
  </view>
</view>